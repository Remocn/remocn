# Introducing remocn implementation

Approved direction: 32 seconds, 1920×1080, 60 fps. The reference analysis and
storyboard were approved by the user on 2026-09-08.

1. Define editable content/theme, the named timeline, and motion curves derived
   from the reference analysis.
2. Build the nine source scenes and shared visual elements. Demonstrate actual
   remocn primitives and keep transitions deterministic when seeking.
3. Add a generated original soundtrack, bundled font dependency, Studio entry,
   and render commands. Keep the installed template independent of this repo.
4. Update registry, preview configuration, docs, and changelog.
5. Verify source types, docs metadata, registry installation, timeline and audio
   invariants. Render scene stills and a complete MP4; inspect and correct.

The writing-plans skill is not installed in the available skill directories;
this explicit implementation checklist serves as the local plan.
