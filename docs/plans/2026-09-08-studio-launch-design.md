# Studio Launch — remocn

User direction: make the next reusable template from `MOOoNMHStUUB_bVo.mp4`, replacing the Commas Studios name/identity with remocn. The provided video is the approved visual direction.

Reference: 480×270, 30 fps, 1065 frames / 35.5 seconds. Master: 1920×1080, 30 fps.

Preserve the editorial structure: sliced/blurred title reveal; black brand lockup; phone feed over an Alpine landscape; Post / Engage / Grow camera cuts; dark community and courses; gated content with night-to-day reveal; white host/editor UI; desktop in a landscape; closing light reveal and black brand hold.

Build editable React/SVG interface layers, content, camera transforms, and time-driven transitions. Replace the original comma symbol with the remocn mark, and all visible original product naming with remocn. Use a clean generated Alpine background so there is no hidden baked-in original interface. Thumbnails can be extracted from the supplied reference and replaced through props; audio stays an optional user-supplied input and is used in the local recreation export.

Timing language: brisk 0.25–0.5s text entrances, smooth high-distance camera travel, deliberate 0.8–1.8s interface holds, continuous pale-blue bottom light, and a quiet final brand hold. All animations are frame-derived, with no CSS animation clock.

## Verification

- Rendered `out/studio-launch-remocn.mp4`: 1920×1080 H.264, 30 fps, 1065 frames / 35.5 seconds, AAC reference soundtrack.
- Reviewed 27 representative timestamps, then checked opening and editor shots from the final MP4.
- All original product wordmarks replaced by the remocn mark/name. Raster thumbnails contain the reference's course artwork; landscape plate generated cleanly without a baked-in product UI.
- Production build, TypeScript, scoped Biome checks, and 21 template/documentation tests passed.
- Installed the generated registry item into a separate ES2020 consumer project: all 15 source files installed and TypeScript passed.
- Fomo corrections completed in the same session: border-local ball trajectory, no perspective/translation during price exit, removed runway shot and corresponding audio segment. Export now has 1108 picture frames / 18.4667 seconds.
