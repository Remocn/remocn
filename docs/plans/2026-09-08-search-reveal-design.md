# Search Reveal

Reference: user-supplied NNhPC00Tu2RlZsEw.mp4, seconds 0–3 only.
Requested result: a separate registry component, construction circles, search
field entrance, and typing `remocn` in English. This concrete reference defines
the implementation direction.

Use the reference's pale background, thin overlapping circles, snap to a small
dash, expanding white pill, right search icon, centered copy, and moving corner
crosses. Replace the source's collage with original SVG contour and floral
geometry on the remocn primary purple panel. Allow the panel and guides to be
hidden separately. End in the completed search state; do not include later
reference scenes.

Keep the source self-contained and deterministic with Remotion frame timing.
Default movement: 90 frames at 30 fps, followed by 18 frames of hold. Longer
copy extends typing and preview duration. Include static reduced-motion pose.
Validate stage boundaries, extended copy, grapheme typing, speed, out-of-order
seeking, and registry integration; render the full clip and alternate controls.
