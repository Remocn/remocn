# Type Fossil

User selected type-fossil after approving cursor-gravity. Wait for review of
this component before committing or starting another one.

Revised after the user said the first version looked broken. Avoid overlapping
different transparent words, oversized camera fly-throughs, and near-plane
layer disappearance. Use a bounded typographic slice: an opaque, readable word
in front, fine contours of the same word behind it. Earlier versions replace
one another with complementary SVG wipes. After the final word arrives,
compress the depth and straighten the type into one flat imprint.

Preserve warm paper, charcoal and purple contours. Expose copy, up to twelve
drafts, density, thickness, typography, palette and speed. Give
every revision 18 reference frames with an 8-frame wipe and derive preview duration from draft
count. All geometry stays in one SVG and scales with its viewBox.

Validate draft order, no double exposure during wipes, bounded geometry,
continuous collapse, deterministic seeking and the final hold. Render the
revision for user review and update docs, registry and controls together.

Latest review: remove the revision counter and progress marks entirely. Use six
default drafts followed by the final word, and double the switching cadence.
