# Brand Guidelines — Form Study

Approved by the user on 2026-09-10: preserve the complete reference choreography and approximately 17.9 seconds, while replacing its brand, copy, logo, palette, typography and imagery with an independent Form Study identity and product collage.

## Direction

An editorial identity film for a fictional object-design studio. Terracotta (`#A64B38`), graphite (`#242321`), stone (`#D6CFC4`) and paper (`#F5F1E9`). An original architectural F-shaped mark made from a vertical slab and two quarter-circle modules. Manrope display typography and Inter supporting labels, supplied locally by Fontsource. No source-brand assets or soundtrack ship with the template.

## Reference timing and structure

The source contains 1072 picture frames at 60 fps (17.866667 seconds); its audio container extends to 17.925805 seconds. Use the complete picture runtime, 1920×1080 at 60 fps. All scene boundaries are expressed on this reference clock and rescaled for other composition frame rates.

| Reference frames | Scene | Reconstruction |
| --- | --- | --- |
| 0–117 | Identity | Centered new mark; short independent tagline appears below |
| 118–305 | Palette | Graphite field followed by three right-to-left color panels |
| 294–619 | Type specimen | Palette pushes away to reveal baseline guides; typing, backspace and rapid weight/style swaps |
| 614–805 | Collage | New still-life photos and editable identity posters sweep in layered columns |
| 806–1071 | Closing | Graphite cut, three flipping tiles resolve into “Make / Ideas / Real.”; wordmark resolves to the mark |

## Content contract

`BrandGuidelines` exposes brand name, opening tagline, guidelines label, typography phrases, three closing words, footer, theme, optional custom logo and photo URLs, optional audio, and reduced-motion mode. Empty/partial collections fall back to complete usable defaults. Default photos are bundled data URIs, not network dependencies. The palette's printed hex labels derive from the theme. Every scene is editable React/SVG markup, never a flattened source movie.

## Integration and verification

Add the installable `brand-guidelines` registry item, lazy website preview, full docs metadata and usage, English changelog, Studio composition with editable schema, and a frame-review command. Rebuild registry payloads and preview manifest. Test timing across fps, deterministic typography, content overrides, import closure and independent content. Inspect representative frames and transition boundaries in a browser-backed Remotion render. Preserve all unrelated in-progress template edits.

## Implementation checklist

- [x] Original still-life assets and documented prompts
- [x] Configurable content, SVG mark and five timed scenes
- [x] Registry, docs, changelog and Studio integration
- [x] Tests, typecheck, generated payloads and visual review

The brainstorming workflow requested a writing-plans skill, which is not installed in this environment. This explicit checklist is the local fallback; the approved design is sufficient to proceed.
